name: termux_hub
description: تطبيق Termux Hub - مجموعة أدوات متكاملة لـ Termux (عربي، RTL)
version: 0.1.0+1
publish_to: "none"
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.3
  fluttertoast: ^8.2.1
  google_mobile_ads: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icon.png
